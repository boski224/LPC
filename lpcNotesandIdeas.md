#1. Review and copy what's value from nc.thesyrex.pl:~/.bashrc 
#2. Same with others.
#3. Let's rebuild cdd function to display names of top directories but store them abs path. 
	Like this:
		1. .lpc		# in stack /home/msyrek/.lpc
		2. projects # in stack /home/msyrek/projects
		3. sage 	# in stack /home/msyrek/projects/sage
		... etc ... 
